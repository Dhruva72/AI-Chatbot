# Archived chatbot files

These are retained compatibility copies. The maintained local-Ollama application and setup instructions are in the project root. See `..\README.md` and run `..\DATA\main.py`.
